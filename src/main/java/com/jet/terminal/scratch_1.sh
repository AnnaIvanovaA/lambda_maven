#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$RNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";ж
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do

    echo "oooo";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;



    asdasd

    zxczxczc

    echo "sdfsdfs"
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
		echo "sdfsdf"



























    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";





		echo "loll"
		echo "33333"
		echo "sdfsdf"











done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
#!/usr/bin/env bash

for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done


for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
for (( i = 0; i < 5; i++ )); do
    set +x;
    echo " fd: \$FNAME=${FNAME}";
    case "$FNAME111" in
        *.+(z|Z|gz))    echo "match .gz"; FNAME="${FNAME:%.+(z|Z|gz)}"; echo "after fname"; DECOMPRESSOR="gzip -dc"; echo "after decompresor" ;;
        *.bz2)          DECOMPRESSOR="bzip2 -dc" ;;
        *.xz|*.lzma)    DECOMPRESSOR="xz -dc" ;;
    esac;
    echo "~fd: \$FNAME=${FNAME}";
done
